api:
    path:  '%modera_direct.routes_prefix%/api.js'
    defaults: { _controller: ModeraDirectBundle:Direct:getApi }
remoting:
    path:  '%modera_direct.routes_prefix%/remoting.js'
    defaults: { _controller: ModeraDirectBundle:Direct:getRemoting }
route:
    path: '%modera_direct.routes_prefix%/route'
    defaults: { _controller: ModeraDirectBundle:Direct:route }
